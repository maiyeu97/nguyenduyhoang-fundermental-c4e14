from turtle import *

def draw_star(x, y, length):
    pos()
    (x, y)
    color("red")
    for i in range(5):
        forward(length)
        right(144)
        forward(length)
draw_star(12.23, 14.54, 200)
mainloop()
